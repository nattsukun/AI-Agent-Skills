# /vibe-spec

**Phase:** DEFINE → SPEC  
**Lead:** BA | **Support:** PO, SA

Write a PRD with testable acceptance criteria before any code.

Load skill: `vibecoding-squad-gsd`  
Playbook: `references/command-playbooks.md` → /vibe-spec

**User prompt template:**

```text
/vibe-spec

Feature: [name]
Product brief: [paste or path]
Tech context: [stack, repo, existing modules]
```

**Output:** Spec document + SPEC GSD card + handoff to /vibe-plan